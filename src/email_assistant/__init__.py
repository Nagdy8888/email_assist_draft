"""
Email assistant package: LangGraph-based agent for email triage, Q&A, and Gmail integration.

Use cases: process incoming emails (triage → notify/respond/ignore), answer user questions,
send new emails on request, and persist chats/memory via Supabase and Postgres checkpointer.
"""

__version__ = "0.1.0"
