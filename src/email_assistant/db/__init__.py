"""
DB layer: store (memory) and checkpointer (PostgresSaver) configuration.

Use cases: get_store(), get_checkpointer() for compile(store=..., checkpointer=...).
"""

# Stub: real store/checkpointer setup added in later phases.
