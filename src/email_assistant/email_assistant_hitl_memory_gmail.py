"""
Entry: build and compile the top-level graph with response subgraph, checkpointer, and store.

Use cases: build graph (START → input_router → email path or question path),
compile with PostgresSaver and store; invoke with email_input or user_message/question.
"""

# Stub: real graph build/compile added in later phases.
