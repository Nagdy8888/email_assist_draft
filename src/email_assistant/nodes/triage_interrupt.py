"""
triage_interrupt_handler: HITL after notify; user chooses respond or ignore.

Use cases: handle interrupt, resume with Command(resume=...) or Command(goto=...);
optionally update triage_preferences memory.
"""

# Stub: real interrupt handling added in later phases.
